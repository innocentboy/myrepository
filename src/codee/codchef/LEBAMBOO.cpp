/**
http://www.codechef.com/problems/LEBAMBOO
*/
#include <cstdio>
#include <iostream>
#include <set>
#include <vector>
#include <algorithm>
#include <cmath>
#include <stack>
#include <map>
#include <cmath>
#include <cstring>
using namespace  std;
#define N 55
#define M 1000000007

int a[N],b[N];

int main()
{
    int i,j,k,t,n;
    scanf("%d",&n);
    while(t--)
    {
        scanf("%d",&n);
        for(i=0;i<n;i++)
            scanf("%d",&a[i]);
        for(i=0;i<n;i++)
            scanf("%d",&b[i]);
        //Now compute the result..
    }

    return 0;
}
